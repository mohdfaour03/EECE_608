training_result_path: results/training/example_training_run.json
audit_regime: passive_final_model_only
auditor_variant: score_fusion_passive
auditor_strength_rank: 2
delta: 0.00001
repeated_seeds: [451, 452, 453]
passive:
  query_budget: 2048
  score_type: score_fusion
  calibration_method: isotonic_like
  calibration_fraction: 0.1
saturation:
  min_absolute_improvement: 0.05
  min_relative_improvement: 0.02
  tightness_ratio_tolerance: 0.01
  use_confidence_interval_overlap: true
run:
  results_root: results
  save_debug_artifacts: true
