# RUN QUEUE — 2026-07-11 (everything still missing from EXPERIMENT_PLAN_2026-07-08)

**Status audit.** Code for E1 is implemented and tested; bundles for E3 (saturation v4),
the sigma sweep v3, and E5 (matched canary v2) were built 2026-07-07/08 but **none of the
plan's experiments have been run yet** — there are no results under `results/hpo/`,
`results/decomposition/`, or `codex/results/{mnist_saturation,sigma_sweep,matched_canary_sweep}/`.
E4 had **no runner at all**; it now does (`codex/run_decomposition_sweep.py`, added today).

**What changed today (2026-07-11):**

- `codex/run_decomposition_sweep.py` — NEW: E4 decomposition sweep (passive Raw-LiRA K=32 +
  matched canary on the tuned E2 models; holdout-Wilson + GDP; telescoping share
  decomposition with gates G2/G3 built in; resume-friendly; also produces the E2
  checkpoints if absent). Share formulas verified to telescope to 1 on synthetic rows.
- `experiments/aggregate_results.py` — extended for E7: emits
  `results/summaries/decomposition_table1.{csv,json}` from E4 outputs (no-op until E4 runs).
- `notebooks/colab_hpo_e1_e2_v1.ipynb` + `hpo_bundle_v1.zip` — NEW: E1+E2 (per-ε Optuna HPO
  + canonical-seed final models), pre-flight test suites + I2/I6 markers, per-cell resume,
  acceptance gates (achieved ε ±1e-3, acc std <2%, G1) baked in.
- `notebooks/colab_decomposition_e4_v1.ipynb` + `notebooks/kaggle_decomposition_e4_v1.ipynb`
  + `decomposition_bundle_v1.zip` — NEW: E4 on Colab and Kaggle (longer sessions).
- `saturation_bundle_v4.zip`, `sigma_sweep_bundle_v3.zip`, `matched_canary_bundle_v2.zip` —
  refreshed with the current `data/datasets.py` (diabetes loader, added 2026-07-08 after the
  bundles were built); saturation notebooks re-pinned to the new bundle sha.

**Bundle sha256 (pinned in the notebooks):**

```
saturation_bundle_v4.zip     754fe0e98433b95bd74ddee20b1674396c5a4220b516eb5d4210861d1746a564
hpo_bundle_v1.zip            44cfbb9e9190d392036f28ae03e4639e37bc36f02e8ac9c64959faf51df935fe
decomposition_bundle_v1.zip  9dcf77389d176661e68308e0c069acac4e0e30f1e33f1cc85f54bc94d313f06d
```

---

## Ordered queue (GPU sessions on Colab T4 / Kaggle)

Execution order per plan §"Execution order and gates": E1 → E2 → {E3, E4, E5} → E6 → E7.

| # | Run | Notebook / bundle | Est. time | Unblocks |
|---|-----|-------------------|-----------|----------|
| 1 | **E1+E2 MNIST** — HPO grid (6 ε-cells × 30 trials) + final models (3 seeds × 6 ε) | `colab_hpo_e1_e2_v1.ipynb` + `hpo_bundle_v1.zip`, `DATASET="mnist"` | ~3–6 h total; per-ε resumable, split across sessions freely | E4, and the models every other audit quotes |
| 2 | **E3 saturation K-ladder** (independent of E1; run any time) | `colab_saturation_k512_v4.ipynb` + refreshed `saturation_bundle_v4.zip` (or Kaggle mirror) | ~1–1.5 h | Whether E4's residual share is *attributable* (PLATEAUED) or "≥ partly weak-auditor" (STILL CLIMBING) |
| 3 | **G3 smoke** (once): in the E4 notebook run `SMOKE=1 ... --dataset mnist --epsilon 1.0` | `colab_decomposition_e4_v1.ipynb` | ~10 min | Confirms the pipeline refuses to certify from tiny budgets (all rows `exploratory`) |
| 4 | **E4 decomposition, MNIST** — one (dataset, ε) cell per session; needs Run 1's results zip | `colab_decomposition_e4_v1.ipynb` or `kaggle_decomposition_e4_v1.ipynb` + `decomposition_bundle_v1.zip` | ~1–2 h per cell × 6 | The paper's Table 1 (per-seed rows + telescoping shares, gate G2 asserted per cell) |
| 5 | **Sigma sweep v3** (regenerates the retracted pre-fix sigma table honestly) | `colab_sigma_sweep_v3.ipynb` + refreshed `sigma_sweep_bundle_v3.zip` | ~2.5–4.5 h | Paper's sigma-sweep section |
| 6 | **E5 matched canary sweep v2** (canary validity case study) | `colab_matched_canary_sweep_v2.ipynb` + refreshed `matched_canary_bundle_v2.zip` | ~2.5–4 h | Paper §Validity (44.3% figure + artifact quantification); paper can ship without it |
| 7 | **E1+E2 diabetes**, then **E4 diabetes** (E6 replication; passive-only cells — no tabular canary design exists yet, rows carry `threat_share_excluded=true`) | same notebooks, `DATASET="diabetes"` | ~4–8 h total | Cross-dataset claims |
| 8 | **E7 aggregation + figures** (local, no GPU): `python experiments/aggregate_results.py` now also writes `summaries/decomposition_table1.csv`; then `paper/figures/make_paper_figures.py` | — | minutes | Paper numbers freeze (after gate G3 rerun of `tests/` + `validate_estimator_scorer_20260705.py`) |

**Handoffs between runs:** Run 1 downloads `hpo_e1e2_results_mnist.zip` → upload it in the E4
notebook (cell 6). E4 downloads `training_results_*.zip` per cell → re-upload next session to
skip retraining targets. On Kaggle, attach bundles as datasets with slugs
`dp-audit-decomposition-v1` and `dp-audit-hpo-results` (instructions in the notebook header).

**Reminders (plan §Explicitly forbidden):** never quote pre-2026-07-06 ε_lower; never quote
the 93% accounting share; tune on utility only; state budgets when comparing ε_lower across cells.
E4's canary track is MNIST-only for now — extending matched canaries to tabular data is an open
item (touches `auditing/canary/generation.py`; do NOT improvise it mid-run).
